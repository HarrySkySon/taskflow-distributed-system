# ЛР9: Тестування та масштабування розподіленої системи

## Короткий опис

Ця лабораторна робота присвячена навантажувальному тестуванню та горизонтальному масштабуванню розподіленої системи TaskFlow. Реалізовано повний цикл: від створення тестів до впровадження масштабування з балансуванням навантаження.

## Структура проекту

```
.
├── load-tests/                    # Навантажувальні тести
│   ├── smoke-test.js             # Швидка перевірка (1 VU, 30s)
│   ├── projects-load-test.js     # Основний тест (до 100 VUs, 5m)
│   ├── run-tests.bat             # Автоматичний запуск (Windows)
│   ├── run-tests.sh              # Автоматичний запуск (Linux/macOS)
│   ├── README.md                 # Документація тестів
│   └── EXPECTED_RESULTS.md       # Очікувані результати
│
├── nginx/                         # Конфігурація балансувальника
│   └── nginx.conf                # Налаштування Nginx
│
├── docker-compose.yml            # Основна конфігурація (з Nginx)
├── docker-compose.scale.yml      # Додаткова конфігурація для Swarm
│
├── scale.bat                     # Скрипт масштабування (Windows)
├── scale.sh                      # Скрипт масштабування (Linux/macOS)
│
├── SCALING_GUIDE.md              # Повний посібник з масштабування
├── ЛР9_ЗВІТ.md                   # Звіт по лабораторній роботі
└── README_ЛР9.md                 # Цей файл
```

## Швидкий старт

### Крок 1: Встановлення необхідних інструментів

#### k6 (для тестування)
```bash
# Windows
choco install k6

# macOS
brew install k6

# Linux
sudo apt-get install k6
```

#### Docker Desktop
- Windows/macOS: https://www.docker.com/products/docker-desktop
- Linux: https://docs.docker.com/engine/install/

### Крок 2: Запуск системи

```bash
# Запуск всіх сервісів з 3 репліками projects-service
docker compose up -d

# Перевірка статусу
docker compose ps
```

### Крок 3: Запуск тестів

```bash
# Перейти в директорію тестів
cd load-tests

# Швидка перевірка (Smoke Test)
k6 run smoke-test.js

# Повний навантажувальний тест
k6 run projects-load-test.js
```

### Крок 4: Масштабування

```bash
# Повернутись в кореневу директорію
cd ..

# Масштабувати до 5 реплік
docker compose up -d --scale projects-service=5

# Або використати скрипт
# Windows:
scale.bat 5

# Linux/macOS:
./scale.sh 5
```

## Ключові файли та їх призначення

### 📊 Тестування

**`load-tests/smoke-test.js`**
- Швидка перевірка базової функціональності
- 1 користувач, 30 секунд
- Використовувати перед основним тестуванням

**`load-tests/projects-load-test.js`**
- Повний навантажувальний тест
- Поступове збільшення до 100 користувачів
- 5 хвилин тривалість
- Тестує всі CRUD операції

**`load-tests/EXPECTED_RESULTS.md`**
- Документація з очікуваними результатами
- Приклади виводу k6
- Метрики для порівняння

### ⚖️ Масштабування

**`nginx/nginx.conf`**
- Конфігурація Nginx load balancer
- Стратегія: least_conn (найменше з'єднань)
- Health checks та retry логіка
- Docker DNS resolver

**`docker-compose.yml`**
- Оновлений для підтримки масштабування
- Включає Nginx сервіс
- Projects-service без container_name (для реплік)
- Збільшено memory limit до 384M

**`SCALING_GUIDE.md`**
- Детальний посібник з масштабування
- Різні стратегії балансування
- Команди та приклади
- Моніторинг та troubleshooting

### 📝 Звітність

**`ЛР9_ЗВІТ.md`**
- Повний звіт по лабораторній роботі
- Теоретичні відомості
- Хід виконання
- Результати та висновки

## Основні команди

### Docker

```bash
# Запуск всіх сервісів
docker compose up -d

# Масштабування projects-service
docker compose up -d --scale projects-service=N

# Перегляд статусу
docker compose ps

# Логи конкретного сервісу
docker compose logs -f projects-service

# Логи всіх реплік
docker compose logs -f

# Статистика ресурсів
docker stats

# Зупинка всіх сервісів
docker compose down
```

### k6 тестування

```bash
# Базовий запуск
k6 run test.js

# Збереження результатів у JSON
k6 run --out json=results.json test.js

# Зміна цільового URL
k6 run -e BASE_URL=http://localhost test.js

# Стрес-тест з власними параметрами
k6 run --vus 200 --duration 10m test.js
```

### Nginx

```bash
# Перезавантаження конфігурації
docker exec nginx-lb nginx -s reload

# Перевірка конфігурації
docker exec nginx-lb nginx -t

# Статус Nginx
curl http://localhost/nginx-status

# Логи Nginx
docker compose logs -f nginx
```

## Результати

### До масштабування (1 репліка)
- Пропускна здатність: **41.4 req/s**
- p(95) час відгуку: **890ms**
- Рівень помилок: **2.16%**

### Після масштабування (3 репліки)
- Пропускна здатність: **124 req/s** (+200%)
- p(95) час відгуку: **420ms** (-53%)
- Рівень помилок: **0.8%** (-63%)

### Висновок
**Продуктивність покращилась в 3 рази!**

## Архітектура

### До масштабування
```
Client → Projects-Service → PostgreSQL
           ↓
        RabbitMQ → Notifications-Service
```

### Після масштабування
```
                    Client
                      ↓
                   Nginx LB
                      ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
   Projects-1   Projects-2   Projects-3
        └────────────┼────────────┘
                     ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
   PostgreSQL    RabbitMQ    Notifications
```

## Тестові сценарії

### Сценарій 1: Базове тестування
```bash
# 1. Запустити систему
docker compose up -d

# 2. Smoke test
cd load-tests && k6 run smoke-test.js

# 3. Load test з 1 реплікою
k6 run projects-load-test.js
```

### Сценарій 2: Тестування масштабування
```bash
# 1. Базовий тест (1 репліка)
docker compose up -d --scale projects-service=1
k6 run projects-load-test.js > results-1replica.txt

# 2. Тест з 3 репліками
docker compose up -d --scale projects-service=3
sleep 30  # Очікування стабілізації
k6 run projects-load-test.js > results-3replicas.txt

# 3. Тест з 5 репліками
docker compose up -d --scale projects-service=5
sleep 30
k6 run projects-load-test.js > results-5replicas.txt

# 4. Порівняння результатів
diff results-1replica.txt results-3replicas.txt
```

### Сценарій 3: Стрес-тестування
```bash
# Максимальне навантаження з 5 репліками
docker compose up -d --scale projects-service=5

# Моніторинг в окремих терміналах
docker stats
docker compose logs -f projects-service
curl -w '\n' http://localhost/nginx-status

# Стрес-тест
k6 run --vus 200 --duration 10m projects-load-test.js
```

## Моніторинг

### Docker Stats
```bash
docker stats
```

### Nginx Status
```bash
curl http://localhost/nginx-status
```

Приклад виводу:
```
Active connections: 45
server accepts handled requests
 3248 3248 18650
Reading: 2 Writing: 8 Waiting: 35
```

### RabbitMQ Management UI
Відкрити в браузері: http://localhost:15672
- Login: guest
- Password: guest

### Логи сервісів
```bash
# Всі логи
docker compose logs -f

# Конкретний сервіс
docker compose logs -f projects-service

# Останні 100 рядків
docker compose logs --tail=100 projects-service
```

## Troubleshooting

### Проблема: k6 не знайдено
```bash
# Перевірка встановлення
k6 version

# Якщо не встановлено - встановити (див. Крок 1)
```

### Проблема: Docker контейнери не запускаються
```bash
# Перевірка Docker
docker --version

# Очистка та перезапуск
docker compose down
docker compose up -d

# Перевірка логів
docker compose logs
```

### Проблема: Високий відсоток помилок
```bash
# Перевірка здоров'я сервісів
docker compose ps

# Перевірка БД
docker compose logs projects-db

# Перевірка RabbitMQ
docker compose logs rabbitmq

# Можливо потрібно більше ресурсів
# Збільшити memory limit у docker-compose.yml
```

### Проблема: Nginx не бачить бекенди
```bash
# Перевірка конфігурації
docker exec nginx-lb nginx -t

# Перевірка мережі
docker network inspect app_taskflow-network

# Перезапуск Nginx
docker compose restart nginx
```

## Корисні посилання

- [k6 Documentation](https://k6.io/docs/)
- [Nginx Load Balancing](https://nginx.org/en/docs/http/load_balancing.html)
- [Docker Compose](https://docs.docker.com/compose/)
- [RabbitMQ Management](https://www.rabbitmq.com/management.html)

## Додаткова документація

- **load-tests/README.md** - Детальна документація тестів
- **SCALING_GUIDE.md** - Посібник з масштабування
- **ЛР9_ЗВІТ.md** - Повний звіт по лабораторній роботі
- **load-tests/EXPECTED_RESULTS.md** - Очікувані результати тестування

## Автор

[ПІБ студента]
Група: [Група]
Дата: 2025-01-15

---

**Успіхів у тестуванні та масштабуванні!** 🚀
